// import axios from 'axios'

export default function ({ $auth, store }) {
  // const host = process.env.baseUrl

  // const wallet = axios.get(`${host}//`, {
  //   params: {
  //     id: $auth.user.id
  //   }
  // }).then((res) => {
  //
  // })

  // store.commit('SET_USER_WALLET', { helloworld: '123' })
}
